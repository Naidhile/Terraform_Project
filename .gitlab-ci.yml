stages:
  - clone
  - setup
  - initialize
  - plan
  - apply
  - test
  - cleanup

variables:
  TF_VERSION: "1.0.0"  # Terraform version to install
  TF_PROJECT_DIR: "/builds/naidhilebhuvana/Terraform_Project"  # GitLab Runner's path

services:
  - name: docker:20.10.7-dind  # Updated Docker version
    alias: docker
    command: ["--host=tcp://0.0.0.0:2375"]

before_script:
  - echo "Installing Terraform..."
  - curl -LO https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip
  - unzip terraform_${TF_VERSION}_linux_amd64.zip
  - mv terraform /usr/local/bin/
  - terraform --version
  - echo "Installing Docker..."
  - apt-get update && apt-get install -y docker.io  # Install Docker in the Ruby container
  - export DOCKER_HOST=tcp://docker:2375
  - echo "Waiting for Docker daemon to initialize..."
  - sleep 30  # Add a short wait time for Docker to fully initialize
  - docker info  # Verify Docker is available
  - docker ps || (echo "Docker is not running correctly" && exit 1)

clone:
  stage: clone
  script:
    - echo "Cloning the repository..."
    - git clone https://github.com/Naidhile/Terraform_Project.git
    - cd Terraform_Project
    - echo "Repository cloned successfully."

setup:
  stage: setup
  script:
    - echo "Setting up Terraform directory..."
    - mkdir -p $TF_PROJECT_DIR  # Ensure the directory exists
    - cd $TF_PROJECT_DIR
    - echo "Setup complete."

initialize:
  stage: initialize
  script:
    - echo "Installing Terraform..."
    - curl -LO https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip
    - unzip terraform_${TF_VERSION}_linux_amd64.zip
    - mv terraform /usr/local/bin/
    - terraform --version
    - echo "Initializing Terraform..."
    - cd $TF_PROJECT_DIR
    - terraform init
    - echo "Terraform initialization complete."

plan:
  image: docker:20.10.7
  script:
    - echo "Installing Terraform..."
    - curl -LO https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip
    - unzip terraform_${TF_VERSION}_linux_amd64.zip
    - mv terraform /usr/local/bin/
    - terraform --version
    - echo "Initializing Terraform..."
    - cd $TF_PROJECT_DIR
    - terraform init
    - echo "Running Terraform plan..."
    - terraform plan -out=plan.tfout -var="container_name=my_container" -var="image=nginx:latest" -var="external_port=8082"

apply:
  stage: apply
  script:
    - echo "Applying Terraform plan..."
    - cd $TF_PROJECT_DIR
    - terraform apply -auto-approve plan.tfout
    - echo "Terraform apply complete."

test:
  stage: test
  script:
    - echo "Running tests..."
    - cd $TF_PROJECT_DIR
    - terraform show
    - echo "Tests complete."

cleanup:
  stage: cleanup
  script:
    - echo "Cleaning up Terraform state..."
    - cd $TF_PROJECT_DIR
    - terraform destroy -auto-approve
    - echo "Cleanup complete."
